=========================
jquery-timesince Releases
=========================

jquery-timesince 0.2.1 (TBD)
============================

* Fixed pulling timestamps from `<time>` elements when stored only on the
  node and not in a `datetime` attribute.
